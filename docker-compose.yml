# ==========================================
# JavaEE 课程管理系统 - 主 Docker Compose
# ==========================================
# 包含所有服务：后端、前端、AI服务、SRS流媒体
# 使用远程 MySQL 数据库（120.26.212.210）
# ==========================================

services:
  # ==========================================
  # SRS 流媒体服务器
  # ==========================================
  srs:
    image: ossrs/srs:5
    container_name: yali-srs
    ports:
      - "8000:1935"   # RTMP 推流端口
      - "1985:1985"   # HTTP API 端口
      - "8088:8080"   # HTTP-FLV/HLS 播放端口
    environment:
      - CANDIDATE=localhost
    volumes:
      - ./docker/srs.conf:/usr/local/srs/conf/srs.conf
      - ./srs-data:/usr/local/srs/objs/nginx/html
    restart: unless-stopped
    networks:
      - yali-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:1985/api/v1/versions"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ==========================================
  # OnlyOffice 文档服务器
  # ==========================================
  onlyoffice:
    image: ${ONLYOFFICE_IMAGE:-docker.1ms.run/onlyoffice/documentserver:7.5}
    container_name: yali-onlyoffice
    ports:
      - "8081:80"     # HTTP 端口
      - "8443:443"    # HTTPS 端口（可选）
    environment:
      - JWT_ENABLED=false
      - JWT_SECRET=yali-secret-key-2024
      - AMQP_URI=amqp://guest:guest@localhost
      - ALLOW_PRIVATE_IP_ADDRESS=true
      - ALLOW_META_IP_ADDRESS=true
    volumes:
      - onlyoffice-data:/var/www/onlyoffice/Data
      - onlyoffice-logs:/var/log/onlyoffice
      - onlyoffice-fonts:/usr/share/fonts/truetype/custom
      - onlyoffice-forgotten:/var/lib/onlyoffice/documentserver/App_Data/cache/files/forgotten
    restart: unless-stopped
    networks:
      - yali-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

# ==========================================
# 网络配置
# ==========================================
networks:
  yali-network:
    driver: bridge
    name: yali-network

# ==========================================
# 数据卷配置
# ==========================================
volumes:
  onlyoffice-data:
    driver: local
  onlyoffice-logs:
    driver: local
  onlyoffice-fonts:
    driver: local
  onlyoffice-forgotten:
    driver: local
